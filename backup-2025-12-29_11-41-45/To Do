1. Give visual feedback when fetching data so the user knows something is happening otherwise users are likely to keep clicking.

2. Fix the Announcements retrieval.

3. Implement filtering for each sheet.

4. Determine custom headers to use for each sheet (such as calculated columns).

5. Create a sidebar interface for common editing functions such as date offsets, search and replace, Publish and others.

6. Check edit storage for all sheets.

7. Change how accommodations editing works. We need some way of storing which students have accommodations and what time. One possibility is creating a hidden assignment with a rubric, but I am wide open to a different method of storing those details. It will allow us to apply the same accommodations for a given student to all assessments that allow accommodations.
