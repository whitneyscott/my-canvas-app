1. Check edit storage for all sheets. Add accommodation syncing to Canvas.

2. Implement spreadsheet-like functionality:
   1. Sticky headers - Make table headers always visible when scrolling (CSS position: sticky).
   2. Freeze panes - Freeze the checkbox column and the "name" column (first data column) so they persist when scrolling horizontally.
   3. Enhanced row selection - Visual highlighting, keyboard shortcuts (Shift+Click for range), selection state persistence.
   4. Fill down / Copy down - Detect cell range selection, add "Fill Down" option (right-click menu or keyboard shortcut), copy value from first selected cell to all others in range.
   5. Paste to multiple cells - Handle paste events, detect selected cells, apply pasted value to all selected cells, support both single value and tab-separated multi-cell paste.
   6. Column visibility - Add eye/eye-slash icons in each header for quick show/hide of individual columns, persist visibility state in localStorage.

3. Create persistent sidebar with bulk operations:
   1. Sidebar contains current editing options and multiselect list of column headers.
   2. "Apply Edits" button for bulk operations on selected columns.
   3. Bulk column visibility controls (show/hide multiple columns at once).
   4. Column selection for bulk editing operations.

---

LONG-TERM PLAN

1. The "Spreadsheet" UI Layer (Enhancing Existing Tabs)
   - Keyboard Navigation: Implementing onKeyDown listeners so users can use Arrow keys and Tab to move between cells.
   - The "Copy Down" (Ctrl+D) Shortcut: Logic to detect the active cell and duplicate its value to the row below.
   - Persistent Sidebar: A slide-out panel that replaces the current "Edit" icon workflow, allowing for bulk-editing of selected rows.
   - Column Divider & Metadata Toggle: CSS/JS to separate editable fields from "noise" (IDs, Timestamps) and a button to hide the noise.

2. The "Smart" Bulk Actions (New Logic)
   - Unified Bulk Move: A sidebar tool to move different item types (Assignments, Quizzes, etc.) to an Assignment Group or create a new group.
   - Outcome Alignment Tool: A workflow to browse course Standards/Outcomes and "mass-assign" them to multiple items or rubrics.
   - Sync Status Tracking: A visual indicator in the table showing if the NestJS backend successfully updated Canvas.

3. The "Top-Tier" Auditing Features (AI + Compliance)
   - AI-Link Repair: Integration with Canvas's link checker that suggests valid alternative URLs using AI when a link is flagged as broken.
   - AI Alt-Text Generator: A specialized view that identifies images missing accessibility tags and provides AI-generated descriptions for the teacher to "Approve."
   - Accessibility Scoring: A dashboard summarizing header-nesting errors, contrast issues, and missing labels across the course.
   - Standard Compliance Audit: A filter that instantly shows which items in the course have not yet been aligned to an Outcome/Standard.